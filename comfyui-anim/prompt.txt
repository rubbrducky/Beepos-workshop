# ============================================================================
#  ComfyUI animation sandbox — motion prompt
# ----------------------------------------------------------------------------
#  Lines starting with '#' and blank lines are IGNORED. Everything else is the
#  motion prompt used by animate.py (image-to-video). Edit the block below,
#  drop a start image in ./input/, then run:  (see animate.py header)
#
#  This describes the MOTION applied to the start frame — not the character
#  (the start image already IS the character). Keep it short and physical.
#
#  Recipe cheat-sheet (validated 2026-07-12, Wan 2.2 TI2V-5B):
#    - Keep eyes OPEN: add "eyes stay open and steady, no blinking" — the model
#      fills closed eyes with the screen colour and "blinks green".
#    - Sharp: 1024 size + 30 steps (defaults). 704/20 is faster but soft.
#    - Motions loop-friendlier when they return toward the start pose.
#    - Examples that worked:
#        Beepo working : "happily turning the big wrench up and down, antenna bobbing"
#        Grabby Walker : "the claw crane arm swinging and lowering to grab, bobbing on its legs"
# ============================================================================

the cute character happily doing its thing with gentle bouncy idle motion, bobbing up and down, its big round black eyes stay open and steady looking forward, no blinking, cheerful, sharp crisp clean, plain blue background
